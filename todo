Get GitHub commits, display sane errors if the repo has no permissions / offline etc.
Never save passwords! Setup oauth tokens and save those instead!
Remove placeholder data and wire in admin panels.
Make DB-sync to couch work.
Show the save / sync stuff only if remote DB connection is good, otherwise show an option to get the modal back.
Add to admin the option to delete / rename teams as well as optionally name the github branch
Be nice to select a github repo from a dropdown once connected?

Setup Pi and clone to new SD cards - how will branching / merging work? Give a git gui to each pi.

Unit test environment for S13?

